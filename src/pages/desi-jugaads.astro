---
import Layout from "../layouts/Layout.astro";
import ContentList from "../components/ContentListLatest.astro";
import data from "../data/desi-jugaads__food_and_groceries.json";
---

<Layout>
	<div>
		<!-- {data.data.map(dataItem => (
		<section class="mb-10">
			<h2 class="text-2xl font-bold text-gray-800 mb-4">{dataItem.emoji} {dataItem.title}</h2>
			{dataItem.data.map(data => <ContentList category={data} />)}
		</section>
		))} -->
	</div>

	<section class="mb-10">
		<h1 class="text-3xl mb-4 font-bold">{data.title}!</h1>
		<p class="mb-2">
			{data.description}
		</p>

		<div>
			{data.data.map((dataItem) => <ContentList category={dataItem} />)}
		</div>
	</section>
</Layout>
